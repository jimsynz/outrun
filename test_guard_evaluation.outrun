trait DivideTrait {
    def divide(lhs: Self, rhs: Self): Self
}

struct MyInteger() {}

impl DivideTrait for MyInteger {
    def divide(lhs: Self, rhs: Self): Self 
    when rhs == 0 {
        lhs
    }
    
    def divide(lhs: Self, rhs: Self): Self {
        lhs
    }
}

def test(): MyInteger {
    let int_zero = MyInteger{}
    let int_one = MyInteger{}
    
    DivideTrait.divide(lhs: int_one, rhs: int_zero)
}