================
Multi-line string SQL example
================

let query = """
SELECT users.name, accounts.balance
FROM users 
JOIN accounts ON users.id = accounts.user_id
WHERE accounts.balance > 100
"""

---

(source_file
  (item
    (let_binding
      (destructure_pattern
        (identifier))
      (expression
        (primary_expression
          (literal
            (multiline_string
              (multiline_string_content))))))))
