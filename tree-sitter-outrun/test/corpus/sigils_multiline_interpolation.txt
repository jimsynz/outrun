================
Sigil with multiline string and interpolation
================

let dynamic_query = ~SQL"""
SELECT users.id, users.name
FROM users
WHERE users.department = #{department}
  AND users.status = #{status}
ORDER BY users.created_at DESC
LIMIT #{limit}
"""

---

(source_file
  (item
    (let_binding
      (identifier)
      (expression
        (primary_expression
          (literal
            (sigil
              (type_identifier)
              (multiline_string
                (multiline_string_content)
                (interpolation
                  (expression
                    (primary_expression
                      (identifier))))
                (multiline_string_content)
                (interpolation
                  (expression
                    (primary_expression
                      (identifier))))
                (multiline_string_content)
                (interpolation
                  (expression
                    (primary_expression
                      (identifier))))
                (multiline_string_content)))))))))